# movie manager

## 概要
electronを利用した動画管理用デスクトップアプリ

## 技術スタック
- electron
- nextjs
- typescript
- tailwindcss

## 機能
- キー項目一覧画面、動画一覧画面、動画詳細画面の3画面構成。
- 動画データはアプリ内には持たない。参照パスのみ。
- アプリでは動画のメタデータの書き込みはしない。動画にひもづくデータはデータベースで保持する。
- 各動画のデータの項目は「タイトル」「カテゴリ」「出演者」「公開年」「公開月日」「評価」「コメント」「ユーザー定義項目(3つまで)」。
- 評価は星5段階。初期値は3。
- 初回起動時に設定画面をモーダルで開く。
- アプリ画面に動画データをドラッグ・ドロップすると、項目入力フォームがモーダルで開く。動画にメタデータがある場合、自動入力される。
- 動画サマリー画像は720px×405px。動画の中間時点のスクリーンショット画像をデフォルトのサマリー画像として自動生成する。
- 動画プレイヤーはOSのデフォルトのアプリを使用する。

### 設定モーダル
- 「ユーザー定義項目」の項目名を設定できる。
- ユーザー定義項目を含む各項目から「コメント」を除いたもの1項目をキー項目としてユーザーが選択できる。
- 設定と登録済みデータをすべて初期化する「データ初期化」ボタンを配置。

### キー項目一覧画面
- 画面の名称は「#{キー項目名}一覧」。
- 各動画の評価とは別に、キー項目の評価も存在する。
- サマリー画像1点、キー項目、評価からなるリスト。
- キー項目、評価でソートできる。
- サマリー画像は、キー項目に紐づいた動画のうち評価が高いもの1点を自動的に選択する。選択はキー項目一覧画面表示時に行われる。
- 評価はマウスクリックにより変更できる。変更時即座に反映。
- 各行をクリックするとキー項目により絞り込まれた動画一覧画面に遷移する。

### 動画一覧画面
- 画面の名称は「動画一覧」。ただしキー項目一覧画面から遷移した場合は「#{キー項目値}動画一覧」。
- サマリー画像1点、「タイトル」「カテゴリ」「登場」「公開年月日」「評価」からなるリスト。ただしキー項目一覧画面から遷移した場合はキー項目を除く。
- コメント以外の各項目でソートできる。
- 公開年でソートする場合は月日も考慮する。
- 評価はマウスクリックにより変更できる。変更時即座に反映。
- サマリー画像をクリックすると動画プレイヤーが起動する。それ以外をクリックすると動画詳細画面に遷移する。

### 動画詳細画面
- サマリー画像1点、各項目からなる。
- サマリー画像をクリックすると動画プレイヤーが起動する。
- 評価はマウスクリックにより変更できる。変更時即座に反映。
- 項目編集ボタンをクリックすると、項目入力フォームがモーダルで開く。

### 項目入力フォームモーダル
- サマリー画像ボタン、各項目フォーム、保存ボタン、戻るボタンからなる。
- サマリー画像ボタンを押すと画像枠内で動画の再生が始まる。動画は早送り、巻き戻し、ポーズ、コマ送り、スライダーによる時間移動操作が可能。
- 保存ボタンを押すとその時点のスクリーンショットがサマリー画像となる。
- 動画を削除する「削除」ボタンを配置。

